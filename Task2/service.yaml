apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  labels:
    app.kubernetes.io/name: scaletestapp
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: scaletestapp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
